$(document).ready(function(){
   $("#slideshow").cycle({ 
        fx:    'fade', 
        timeout: 6000,
        pager: "#slideshow_pager",
        pause:         1,
        cleartypeNoBg:   true,
        speed: 500 
        }); 
    }); // eof doc ready
